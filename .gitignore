# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Node / build tools (if added later)
node_modules/
npm-debug.log*

# SASS cache
.sass-cache/
*.css.map

# WordPress uploads — never commit these
uploads/

# Sensitive config — NEVER commit these
.env
*.env
wp-config.php

# Dev/deploy tooling — not needed on production
deploy.sh
setup-server.sh
.env.example
.wp-env.json
README.md

# Import data — only needed for one-time WP import
packages-import/
